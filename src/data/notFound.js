/**
 * What: copy shown on the 404 page.
 * Data from: hardcoded below.
 * Used by: src/pages/Page404.jsx (the only consumer of `notFound`).
 */
export const notFound = {
  code: "404",
  message: "Route Not Found",
  subtitle:
    "The resource you are looking for has been re-indexed, moved, or never existed.",
  before: "Let's get you back to stable production. Head over to the ",
  linkText: "homepage",
  linkTo: "/",
  after: " to find what you need.",
};
